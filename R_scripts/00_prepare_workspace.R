# check working directory
getwd()

# check where R load libraries
.libPaths()

# install packages
# install.packages("raster")
# install.packages("TraMineR")

# load packages
library(TraMineR)
library(raster)
